{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run db:migrate && next build",
  "crons": [
    {
      "path": "/api/cron/sortie-sweeper",
      "schedule": "0 7 * * *"
    }
  ]
}
